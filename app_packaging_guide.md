# NPS 封装与持久化运行指南

如果您觉得每次都要打开终端敲命令启动 `./nps` 或者 `./npc` 太麻烦了，可以通过以下几种方式将其“封装”成更易用的形式。

## 方案一：封装成 macOS 的双击可执行图标程序 (.app)

这是一种非常符合 Mac 用户习惯的方式。我们可以利用 Mac 自带的“自动操作 (Automator)”或“脚本编辑器”把它做成一个像普通软件一样的图标。

### 制作步骤（以客户端 npc 为例）：

1. 在 Mac 系统的启动台中搜索并打开自带应用 **「脚本编辑器」 (Script Editor)**。
2. 确保在主界面中新建了一篇空白文稿。
3. 复制以下这段代码，并粘贴进脚本编辑器中（请根据您实际存放的位置和 `vkey` 修改下面第一行）：
    ```applescript
    # 假设您的项目文件夹在桌面上，并且 vkey 是 abc123xxx
    set npcCommand to "/Users/yyyz/Desktop/nps-master/npc -server=127.0.0.1:8024 -vkey=abc123xxx -type=tcp"

    # 让程序在后台静默运行
    do shell script npcCommand & " > /dev/null 2>&1 &"
    
    display notification "内网穿透客户端 (NPC) 已启动并在后台运行" with title "NPS 客户端"
    ```
4. 点击脚本编辑器的菜单栏：**文件 -> 导出...**。
5. 在弹出的窗口中：
   * **导出为**：填入你想取的名字，比如 `启动穿透.app`
   * **位置**：选择保存到“桌面”或者“应用程序”文件夹
   * **文件格式**：选择 **“应用程序”**
6. 点击**保存**。

现在，您的桌面上就会出现一个叫 `启动穿透.app` 的程序！**以后只需要双击这个图标，客户端就会静默在后台启动了**，不再会跑出来一个黑黑的终端窗口惹人烦。

---

## 方案二：注册为开机自启的系统服务（最推荐、最省事）

这是最专业的封装方式。NPS 官方程序其实已经自带了这个功能。将其注册为系统服务后：它会跑在系统最底层，并且**每次您开机，它就会自动在后台默默启动**。

### 服务端 (NPS) 注册服务：
打开终端，确保您在项目根目录 `/Users/yyyz/Desktop/nps-master` 下：
```bash
# 1. 安装服务（注意：需要获取管理员 root 权限，会提示输入您的 Mac 开机密码）
sudo ./nps install

# 2. 启动服务
sudo nps start
```
*(注意：安装为服务后，nps 默认不再读取桌面的 `conf/nps.conf`，而是会尝试把配置文件复制到系统目录 `/etc/nps/conf` 下面。如果您之前改了端口，请务必保证 `/etc/nps/conf/nps.conf` 里面的端口配置是对的。它的运行日志会被打到 `/var/log/nps.log` 中。)*

**其他常用命令：**
* 停止服务：`sudo nps stop`
* 卸载服务：`sudo nps uninstall`

### 客户端 (NPC) 注册服务：
同理，您也可以把那个带着一大串参数的客户端命令注册进系统服务，达到开机自动连上隧道的目的：

打开终端执行以下命令（注意将最后的 `-server` 和 `-vkey` 换成您自己的）：
```bash
sudo ./npc install -server=127.0.0.1:8024 -vkey=abc123xxx -type=tcp

sudo npc start
```
