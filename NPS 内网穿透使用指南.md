# NPS 内网穿透使用指南

假设您的本机（运行了 `nps` 服务端）既是服务端又是客户端，并且您希望将本机上运行的某个本地服务暴露给外部访问。

NPS 主要提供了两种最常用的穿透方式：**TCP 隧道** 和 **域名解析（主机）**。

---

## 方式一：TCP 隧道穿透（适合任意协议，如 SSH、数据库、本地 Web 开发等）

如果您希望通过 `IP:端口` 的形式直接访问本地服务，请配置 TCP 隧道。

### 第一步：在 Web 控制台创建客户端
1. 在浏览器打开并登录 Web 控制台 (http://127.0.0.1:28080)，默认账户：`admin` / `123`。
2. 在左侧菜单点击 **“客户端”**。
3. 点击 **“新增”** 按钮。
4. 填写“备注”（例如：`我的Mac本机`），其他选项保持默认，点击 **“新增”** 保存。
5. 在刚刚新增的记录中，找到 **“唯一验证密钥 (vkey)”** 并复制它（例如：`abc123xxx`）。

### 第二步：在本机启动 NPC（客户端）
打开一个新的终端窗口，在项目根目录 `/Users/yyyz/Desktop/nps-master` 下执行以下命令连接到刚才创建的 nps 服务端：

```bash
# 替换后面的 vkey 为您刚才复制的密钥
./npc -server=127.0.0.1:8024 -vkey=abc123xxx -type=tcp &
```
> 执行后，您可以回到 Web 控制台的“客户端”页面刷新，应该能看到该客户端的状态变成了**在线**（绿色）。

### 第三步：在 Web 控制台配置 TCP 隧道
1. 在 Web 控制台左侧菜单点击 **“TCP 隧道”**。
2. 点击 **“新增”** 按钮进行如下配置：
   *   **客户端 ID**：填写您在第一步中创建的客户端的 `ID` 数字（可以在“客户端”列表的第一列找到）。
   *   **服务端端口**：输入一个您希望**外部访问的端口**（例如：`9090`，请确保该端口不被占用）。
   *   **目标 (IP:端口)**：输入您本机实际运行的**本地服务地址和端口**，因为是本机，所以填 `127.0.0.1:3000`（假设本地 Web 服务在 3000 端口）。
3. 点击 **“新增”** 保存配置。

## 方式二：域名解析（主机）（适合 HTTP/HTTPS 网站穿透）

如果您在“客户端”列表中点击了右侧的 **“主机”** 按钮，这实际上跳转到的是 **“域名解析”** 的配置页面。
它的作用是：**通过不同的域名，访问内网中不同的 Web 网站项目。**（这省去了记住各种端口号的麻烦，相当于 Nginx 的反向代理功能）。

### 配置步骤：
*(前提：您必须已经拥有了一个公网服务器，并且将您的域名（例如 `*.yourdomain.com`）解析到了该服务器的公网 IP 上。并且您已经按照上面的"第一步"和"第二步"保持客户端 `npc` 处于在线状态。)*

1. 点击客户端列表中的 **“主机”** 按钮（或左侧菜单的 **“域名解析”**）。
2. 点击 **“新增”** 按钮进行如下配置：
   *   **客户端 ID**：选择或确认您的客户端 ID。
   *   **主机（域名）**：填写您准备用来访问本地域名。例如：`test.yourdomain.com`。
   *   **目标 (IP:端口)**：输入您本机实际运行的本地 Web 服务地址。例如 `127.0.0.1:8080`（假设您本地正跑着一个 Vue/React 项目）。
3. 保存后即可。

### 如何访问：
配置完成后，您在外网通过浏览器访问网址 `http://test.yourdomain.com:18080` （注：如果您在云服务器上的 NPS 配置中没有改掉默认 80 端口，那就可以不加端口直接访问 `http://test.yourdomain.com`）。此时流量会穿透到您的本地 `127.0.0.1:8080` 网站。

---

## 进阶功能详细使用步骤

除了基础的端口映射，以下是其他高级功能的具体操作流程：

### 1. UDP 隧道（适合游戏联机/DNS）
*   **适用场景**：游戏服务器（如 Minecraft 联机）、DNS 查询等使用 UDP 协议的服务。
*   **配置步骤**：
    1.  在 Web 控制台点击左侧 **“UDP 隧道”** -> **“新增”**。
    2.  **客户端 ID**：输入您的在线客户端 ID（如 `2`）。
    3.  **服务端端口**：外部访问时使用的端口（如 `9988`）。
    4.  **目标 (IP:端口)**：本地服务地址（如 `127.0.0.1:19132`）。
    5.  **使用**：在游戏或客户端中连接 `服务器IP:9988` 即可通过 UDP 通信。

### 2. SOCKS5 代理（VPN 效果/借用内网网络）
*   **适用场景**：身在外地想用家里的网、或者服务器在海外想用来翻墙。
*   **配置步骤**：
    1.  点击左侧 **“SOCKS 代理”** -> **“新增”**。
    2.  **客户端 ID**：输入发出流量的客户端 ID（如 `2`）。
    3.  **服务端端口**：代理服务器的端口（如 `1080`）。
    4.  **保存**后，隧道即建立。
*   **如何连接使用**：
    *   **浏览器使用**：推荐安装 `SwitchyOmega` 插件，添加 SOCKS5 代理，填入 `服务器IP` 和 `1080`。
    *   **手机使用**：在 WiFi 设置中点击“配置代理” -> “手动”，服务器填 `服务器IP`，端口填 `1080`（注意：部分手机只支持 HTTP 代理，若不支持 SOCKS5，请改用 NPS 的 **“HTTP 代理”** 菜单尝试）。

### 3. 文件访问（内网传文件/极简网盘）
*   **适用场景**：临时向外网分享本地文件夹内的文件。
*   **配置步骤**：
    1.  点击左侧 **“文件访问”** -> **“新增”**。
    2.  **客户端 ID**：指定哪台电脑上的文件。
    3.  **服务端端口**：网页访问端口（如 `8888`）。
    4.  **本地路径**：填入电脑文件夹的绝对路径（如 `/Users/yyyz/Movies`）。
    5.  **使用**：在任何地方浏览器输入 `http://服务器IP:8888`，即可看到文件列表并下载。

### 4. P2P 连接（极速直连，不限速）
*   **适用场景**：两台内网设备（A 和 B）直接传输大文件。
*   **配置步骤**：
    1.  点击左侧 **“P2P 连接”** -> **“新增”**。
    2.  设置一个 **“唯一标识”**（如 `myp2p`）和 **“密码”**（如 `123456`）。
    3.  **操作**：在另一台内网电脑上，除了运行普通的 `npc`，还需要按照控制台给出的 **P2P 启动指令** 运行，这样两台电脑就会尝试跳过服务器直接握手通信。

### 5. 私密代理（极致安全）
*   **适用场景**：不希望在公网上公开暴露任何端口。
*   **配置步骤**：
    1.  点击左侧 **“私密代理”** -> **“新增”**。
    2.  设置 **“密码”**。
    3.  **使用**：访问者也需要运行一个 `npc` 客户端，并使用特殊的 `-type=secret` 模式和密码才能对接上该隧道。
